{
  "name": "cauldron-rush-parti",
  "version": "0.1.4",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/build.mjs",
    "build:vite": "vite build",
    "typecheck": "tsc --noEmit",
    "test": "node scripts/test.mjs",
    "package": "npm run build && cd dist && zip -qrFS ../cauldron-rush.parti.room.zip .",
    "ui:check": "node scripts/ui-check.mjs",
    "verify": "npm run typecheck && npm test && npm run ui:check && npm run build"
  },
  "dependencies": {
    "@parti/worker-sdk": "^0.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.6.3",
    "vite": "^6.0.5"
  }
}
